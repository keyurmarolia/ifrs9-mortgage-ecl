# Local database

Running the pipeline creates `ifrs9_ecl.sqlite3` in this folder. The database is excluded from version control because it contains source loan identifiers and exceeds normal Git hosting limits.

The published CSV and Excel reporting outputs use anonymized account aliases.
